+++
title = "the bottom band: padding above the system bar, send text sits low, priority does nothing on tap, effort is greyed for no stated reason, and the controls should fit one row; the list pane is only a few lines tall"
created = 1788935328
updated = 1788935328
priority = 2
root_commit = "b8421205e882caeadc666ccff26464e4e0f60dda"
tags = ["usability-r3"]
+++
Operator screenshot 2026-09-08 (campaign rounds/3/shots/phone-complaints/bottom-band-0.0.4.png), gpt-5.6-sol on the openai-chatgpt row. Six items, one close each or grouped as they share files: (1) a few points of padding under the last control row so it does not ride the system navigation bar (respect the inset); (2) the 'send' label is kerned low in its box — centre it vertically like the message field's text; (3) 'priority: on' does nothing when tapped — it must toggle (and say what it toggles: the litany priority/effort tier per bl-23bd), or if the provider row cannot take it, be greyed AND say why on tap; (4) 'effort: medium' is greyed with no stated reason — greying must carry a reason (a tap shows 'this row does not take effort' or similar), and if the row DOES take it (gpt-5.6 on openai-chatgpt takes reasoning effort), it must not be greyed — read the provider row's declared knobs from the engine's answer, never from the model name; (5) shrink the controls (smaller chips, shorter labels, the provider and model pickers sharing one row) until stop / stop all / effort / priority / provider / model fit on one row at the phone's width, with the two pickers eliding their text; (6) the transcript/list pane is only a few lines tall on the conversation screen when the keyboard is closed — the bottom band takes too much of the screen; give the transcript everything above the band, the band sized to its content, and let the composer grow only when focused.
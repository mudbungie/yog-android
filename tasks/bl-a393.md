+++
title = "the roster has no doctor: PROTOCOL 17's control op is on the parity ledger, not on the glass"
created = 1788750915
updated = 1788750915
priority = 2
root_commit = "b8421205e882caeadc666ccff26464e4e0f60dda"
tags = ["usability-r1"]
+++
yog PROTOCOL 17 added `doctor` (yog bl-28f4): one read that answers *is this box wired up*, classed `control` in the help roster, so every seat owes it a discoverable interactable (PARITY §5).

The re-vendor (bl-ee21) recorded it as an UNBUILT line in `parity.toml` and a `Refuses(UNSENT)` row in `tests/conformance/replies.rs`; this ball builds the surface and deletes both.

What it wants: the reply is rows of `{check, fact, ok, remedy?}`, world-wide with an optional workspace narrowing — so it is a top-depth read like the trail (DESIGN §13.8), reached from the roster, asked by opening it and by nothing standing. A failing row's `remedy` is the sentence the operator acts on and must be painted whole rather than elided.
+++
title = "the bottom bar says 'select model' instead of the model the conversation is on: every session setting (model, priority, thinking level, …) shows its current value, greyed where the engine does not offer it"
created = 1788751767
updated = 1788751767
priority = 2
root_commit = "b8421205e882caeadc666ccff26464e4e0f60dda"
tags = ["usability-r3"]
+++
Operator request 2026-09-06. On the phone the conversation's bottom controls read as verbs ('select model') rather than states. Each session setting the boundary carries — the model (from the agent/context read), priority and thinking/effort level (litany effort:, yog PROTOCOL 6 role config, bl-23bd), and any other per-conversation knob the admin surface exposes — paints its CURRENT value in the control itself, and is greyed (still readable, not tappable) where the engine's offer set for that conversation or the provider row does not carry it (e.g. priority on a provider that has no tier; thinking level on a model that has none). Tapping opens the chooser as today. Read the engine's offers rather than guessing availability from the model name; where the wire lacks a fact needed to grey correctly, file the yog ball and cite it.
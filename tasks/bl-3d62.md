+++
title = "the punched wire's phone half: rendezvous material beside the leaf, a pure DHT client, the four-rung dial ladder, held punched streams, and redial as the feature (yog bl-0247, REMOTE §13.2–§13.4)"
created = 1790392669
updated = 1790392670
claimant = "Urinalyses-S"
priority = 2
root_commit = "b8421205e882caeadc666ccff26464e4e0f60dda"
+++
The app half of yog bl-0247 (REMOTE §12.1: nothing owed a phone not owed a laptop; §13.2–§13.4, §13.7 ruling 2: reimplement per component). Mirror the engine's client contract byte for byte: material = engine rendezvous public key + pairing salt beside ca.pem; four HKDF-SHA256 derivations under salt 'yog rendezvous' (presence salt, inbox salt, seal key, inbox key); presence at (engine key, presence salt), the call at (inbox key, inbox salt); sealed items nonce(12)||ciphertext||tag(16) over an endpoint list (count, family byte, address, big-endian port), a call prefixed by an 8-byte nonce; the punch (listen and connect from one port, v6 first, keep the first stream); the ping rule ({"ping":true} discarded, two minutes of silence hangs up). Deliver: (1) material grows the two files, both-or-neither; (2) the ladder — held stream, direct address, re-punch at RAM-cached endpoints, full rendezvous — under the same TLS verification whichever rung answered; (3) redial re-enters the ladder with bounded backoff, a network change re-punches then rendezvouses; (4) held streams discard pings and hang up after two minutes of silence. Tests: JVM-free host tests against a fake DHT on loopback UDP and a scripted engine socket; an injected clock for the silence bound.
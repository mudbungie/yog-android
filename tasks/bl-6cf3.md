+++
title = "conversation search is in-wire and unsurfaced: the /search op the parity gate found, and the stale ledger premise behind it"
created = 1788397383
updated = 1788397383
root_commit = "b8421205e882caeadc666ccff26464e4e0f60dda"
+++
Found by the bl-fe4c parity gate, at the moment DESIGN §13.4's hand-kept ledger was folded into the machine-checked parity.toml.

§13.4 files conversation search under **Upstream asks** — "the wire does not carry the fact; a ball on the server's board, not a shim here". That premise is FALSE against the protocol-7 roster. The engine's help table carries `search`, classed `control`, and its own summary is: find the text anywhere — balls, workspaces, conversations, transcripts. So the fact IS in-wire, every seat owes it a discoverable interactable, and this client surfaces none.

That is also the first thing the fold bought: a prose ledger cannot rot loudly, and this line had rotted quietly. The gate now records the absence as a cited exemption instead, which is a loud TODO rather than a wrong sentence about upstream.

What this ball is: give the seat a search surface, and delete the exemption line (deleting it re-reddens the gate, which is the severability test). The codec's own decision table records `search` as Refuses(READ) — a world read outside the chat-loop slice (DESIGN §2) — so this is a slice decision first: does a phone seat search the world, and at which depth does the affordance live. If the answer is no, the exemption's citation changes from 'unbuilt' to the ruling and stays.

Blocked on nothing. The exemption line cites this ball.
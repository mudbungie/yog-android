+++
title = "an open dropdown's popup extends into the gesture-nav dead zone: the list paints where taps cannot land"
created = 1788405013
updated = 1788405021
claimant = "Popup"
priority = 1
root_commit = "b8421205e882caeadc666ccff26464e4e0f60dda"
+++
Operator report (2026-09-03, paraphrased): the context-size dropdown still opens under the nav buttons into dead space. 'Still' because the class has been fixed twice at other sites: bl-192c moved the control BANDS out of the nav area when space runs out, and bl-9cfd capped the composer's scroll above it — but a ComboBox POPUP is neither a band nor the composer: when the selector row sits low (a phone with the tuning band near the bottom), the opened list extends downward past the src/shell/inset.rs floor into the zone src/shell/inset.rs:5 itself documents as 'where taps never reach'.

Fix the CLASS, not one instance: every ComboBox in the shell (effort in controls/tune.rs:38, provider pick.rs:34, model pick.rs:84, and whichever one the operator's 'context size' names — identify it and say so in the close note) must open its popup fully inside the tappable area — above the control when below-space minus the inset cannot hold the list, or height-capped with its own scroll. egui's popup positioning is steerable; controls.rs:91's own comment already records that a ComboBox does not declare its geometry to layout checks, which is the same blindness one level down.

The ratchet, per the house rule (PNGs for eyes, invariants for the gate): add a screens/parity-adjacent assertion that an OPENED dropdown's popup rect stays inside the inset-adjusted tappable area — the walk can open each combo and read the rect off the accessibility/probe layer or the frame. Without the assertion this class gets fixed a third time.

Verify premises against the tree first; the shell moved tonight (rungs 1-3, in-doubt paint).
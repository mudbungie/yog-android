+++
title = "the roster paints attention marks and offers no gesture that answers them (/seen)"
created = 1788397386
updated = 1788583740
claimant = "Animations-W"
root_commit = "b8421205e882caeadc666ccff26464e4e0f60dda"
+++
Found by the bl-fe4c parity gate while classing the 48 absent control ops.

The workspace roster and the conversation list both paint an attention mark (● when `attention > 0`, src/shell/screens.rs), so this seat SHOWS the attention queue. The engine's `seen` op — "answer the selected conversation's place in the attention queue" — is classed `control`, and nothing in this app fires it. A mark an operator can see and cannot clear is a surface that only accumulates.

It is one of twenty-seven ops the codec's decision table records as Refuses(ACT) — a world act outside the chat-loop slice (DESIGN §2) — but it is not like the other twenty-six: the others act on world state this seat never paints, and this one answers a mark this seat paints on two screens. That asymmetry is what makes it worth its own ball rather than a line in the same group.

What this ball is: decide whether the phone answers attention (`seen`, and possibly `attention` beside it as the cross-workspace read), and either surface it — the mark itself is the obvious control — or keep the exemption with a ruling as its citation instead of the group reason.

The parity.toml line cites this ball.
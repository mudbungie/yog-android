+++
title = "upstream patches: the three defects the client carries shims for"
created = 1787548957
updated = 1787549519
claimant = "Concerto"
root_commit = "b8421205e882caeadc666ccff26464e4e0f60dda"
+++
Two of the three are FILED upstream; the third is prepared and waits on an operator browser act.

1. winit wake arm — FILED: https://github.com/rust-windowing/winit/pull/4673 (branch android-text-event-redraw on the mudbungie fork). Master restructured the tree into per-platform crates since 0.30; the arm now lives in winit-android/src/event_loop.rs and the mechanism is unchanged (redraw_flag read in the same loop iteration). Changelog entry included; compile-checked against aarch64-linux-android. Exit when merged+released: consume the release and delete the focus-gated fast-repaint poll (DESIGN §3 records this as the poll's dissolution).

2. android-activity null guard — FILED: https://github.com/rust-mobile/android-activity/pull/252 (branch game-text-null-buffer on the mudbungie fork). The guard is the empty-slice fallback before from_raw_parts; compile-checked for the game-activity feature on aarch64. Exit when released: the Makefile apk comment's 'release profile is load-bearing' warning weakens to an optimization note (debug builds stop aborting on frame one).

3. games-activity restartInput — CANNOT be filed from this box: the library's tracker is Google IssueTracker (AGDK component), browser auth only, and the source is AOSP gerrit, not GitHub. The paste-ready report follows in the next comment. Exit when fixed upstream: delete the GtiAccess/OnKeyListener shim from android/.

No branch debris on this repo's origin: both contributions live on the fork.

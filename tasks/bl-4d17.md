+++
title = "the conversation list's indent hierarchy is clumsy: replace it with L-shaped threading connectors (reddit style) between parent and child rows, consistent per depth, rows never overflowing the screen"
created = 1788753119
updated = 1788753202
claimant = "Cantaloups-A5"
priority = 2
root_commit = "b8421205e882caeadc666ccff26464e4e0f60dda"
tags = ["usability-r3"]
+++
Operator request 2026-09-06 with a phone screenshot (campaign repo rounds/3/shots/phone-complaints/). Today a child row is a full-width box shifted right by depth, with inconsistent offsets (siblings at different indents), root rows centred and children left-aligned, and long rows clipped off the right edge. Ship: one connector glyph column at the left — an L-shaped elbow (└) from the parent's row down to each child, a vertical rule (│) continuing past children that have later siblings, one indent unit per depth — the reddit/HN threading idiom; row boxes keep the same left edge as their parent's text minus the connector width, elide their text with … so nothing clips, and align all row text left. Error rows keep their colour but show a one-line status, not the raw error text (the full text is on the conversation screen). Prove with emulator screencaps before/after on a fixture with three depths and mixed siblings (yog fixture busy plus hand-laid worktrees, as bl-06d3 did).
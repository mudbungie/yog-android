+++
title = "the wire moved to protocol 5: re-vendor and speak it, or the handshake refuses the released engine"
created = 1788319838
updated = 1788319838
priority = 2
root_commit = "b8421205e882caeadc666ccff26464e4e0f60dda"
+++
yog's PROTOCOL moved 4 -> 5 in their bl-e654 (05ac8d14): reply/governing dropped `branch`, gained `follows` and `diverged_lineages`, and its `oid` changed meaning — REMOTE §9.12, litany's follow-the-tip ruling reaching the wire. §3's bump condition is 'what a spelling already in use is taken to say', and `oid` is exactly that: it named the fork commit an agent's branch left, and now names the followed lineage's head.

This client does not spell `governing`, so the expected cost here is a ledger-only move — but the §3 preface gates on the NUMBER regardless of which shapes a seat reads, so the protocol-4 build refuses the released engine and the released engine refuses it. Same shape of work as bl-e837 and no feature attached: re-vendor `corpus/` from a yog checkout at 05ac8d14 or past it, raise `hello::PROTOCOL` to 5 with the section cited, and confirm — never assume — that strict decode and the conformance replay stay green over the moved shape and that the recorded decision for `governing` still reads. The §14 cache's own stamp discards what the protocol-4 build stored, by the mechanism bl-e837 already verified.
+++
title = "a modern look for the phone: one visual language (soft neon accents, no bordered boxes, hierarchy from layout and type, state by colour) applied to every screen"
created = 1788753582
updated = 1788753582
priority = 2
root_commit = "b8421205e882caeadc666ccff26464e4e0f60dda"
tags = ["usability-r3"]
+++
Operator request 2026-09-06: 'move towards a more modern look: less ugly boxes, more soft neon and interface that makes sense just because of layout.' Design first, then implement. The language has ONE home (docs/STYLE.md in this repo, with the desktop seat citing it later): a dark ground with 2–3 elevation tints instead of bordered boxes; a small neon accent palette used only for STATE (live/in-flight, waiting-on-you, held, error, at rest) at low saturation ('soft neon': glow/tint, never full-saturation fills); hierarchy from spacing, type scale and alignment, not from borders; touch targets ≥48dp; rows without outlines, separated by spacing and a hairline at most; the threading connectors (bl-4d17) and the workspace bar (bl-6e8a) adopt the same tokens. Implement as one egui theme/tokens module every screen reads (no per-screen colours), then restyle: roster, conversation list, chat (bubbles → aligned blocks with a left accent rule), composer and bottom controls (bl-809d), admin surfaces. Prove with emulator screencaps of every screen before/after into the campaign repo rounds/3/shots/design/. Coordinate: lane A5 is landing bl-4d17/bl-6e8a in the list and bar files — land the tokens module first, then restyle those files after A5's close.
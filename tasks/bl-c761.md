+++
title = "the shell: egui/NativeActivity module, APK build recipe, and the CI android leg"
created = 1787545591
updated = 1787549073
claimant = "Concerto"
root_commit = "b8421205e882caeadc666ccff26464e4e0f60dda"
+++
UNBLOCKED: operator approved the shell dependency set 2026-08-23 (eframe, winit, jni, log, android_logger — the versions the spike proved). Posture ruling recorded with it: the operator holds this CLIENT to a laxer dependency bar than the core engine — rule 6's approval step stands, but approvals here are expected to be cheap. Input mechanism to build: the GameActivity + buffer-diff bridge as proven under bl-014e (read its comments for the full trap ledger: release-profile-only, INTERNET permission, v3 certs, insets by JNI, GLES-only, cursor via TextEditState, focus-gated repaint).

---

KICKOFF DOSSIER for the implementing agent. Read this ball's whole journal
plus closed bl-8d03/bl-014e (bl show <id>) before writing code — the traps in
them each cost a real debugging session. Session start: bl prime --as
YOUR_OWN_IDENTITY (never reuse another agent's), then work only in the claim
worktree.

WHAT LANDS (suggest splitting on these seams if any file family projects
large; each close pays the full gate):
1. Shell skeleton: src/shell/* (android glue: android_main, GLES-forced wgpu,
   JNI ime-inset probe, the two-way mirror bridge with cursor sync, per-field
   set_ime_editor_info, echo-window guard) — mined from
   ~/dev/yog-android-spike/src/lib.rs, which is throwaway reference, not
   gospel. tarpaulin.toml excludes src/shell/* WITH the reasoning written
   there (yog's src/shell precedent). rust-toolchain.toml gains the
   aarch64-linux-android target.
2. The gradle shell, in-repo (android/ dir): mine
   ~/dev/yog-android-spike2/app — manifest MUST keep the INTERNET permission
   (its absence reports as DNS failure, not permission denial), the
   games-activity 4.4.0 + appcompat + kotlin-bom dependency trio, the
   MainActivity + GtiAccess Java shim (the backspace-repeat fix; ~40 lines).
   TRAP: do NOT commit a gradle-wrapper jar — the leak gate rejects any
   binary (BINARY_ALLOWED matches nothing) and that is correct; require
   system gradle and document it in the Makefile apk target + README.
3. Makefile apk target: cargo ndk (aarch64) --release into jniLibs, then
   gradle assembleDebug. RELEASE PROFILE IS LOAD-BEARING, not an
   optimization: android-activity 0.6.1 aborts under debug-assertions on the
   pre-IME null buffer (bl-014e).
4. CI android leg beside the host gate: install/pin NDK + cargo-ndk +
   aarch64 target, build the .so and the APK; build-only (no device on a
   runner). Keep actions SHA-pinned like ci.yml.

DEPENDENCIES (operator-approved 2026-08-23, client held to a laxer bar than
core): eframe 0.36 (features wgpu, android-game-activity, default_fonts —
NEVER android-native-activity, the backends conflict), winit 0.30
(android-game-activity), jni 0.21, ndk-context 0.1, log 0.4, android_logger
0.15. serde_json and rustls are already in. Grow deny.toml's license list
only to what the lockfile then names, one comment each.

THE ONE OPEN DESIGN CHOICE (decide, record in DESIGN §3): input wake. The
spike's fix is a 3-line TextEvent arm in a vendored winit
(~/dev/yog-android-spike/vendor/winit) — but THIS repo is registry-only, no
git/path deps, no exceptions standing. Ladder: (a) ship focus-gated fast
repaint (16ms while focused) and make sure the focus flag is NOT reset
before the repaint decision — that inversion silently reverted the fix once;
(b) file the upstream winit PR (bl-e161 tracks it) and consume the release
when it lands; (c) ask the operator to rule an interim exception. Do not
take (c) silently.

UNSAFE: the JNI probe needs it. Replace unsafe_code=forbid with the ast-grep
location rule AGENTS.md rule 3 already prescribes — one named file, its
soundness argument written in it, fixture updated, rules-audit both
directions.

VERIFICATION: the gate covers the host-testable core; on-device verification
is operator-assisted — ASK the operator to run the APK rather than driving
their phone unprompted, and never touch the device unless the app itself
holds the screen focus. Provisioned material on-device lives in the app's
private files/wire (ca.pem, client.pem, client.key, address); registration
is a server-side operator act; an unregistered leaf correctly sees empty
rows.

---

Correction to the dossier: the upstream-PR tracking ball is bl-2958 (the dossier guessed an id before it existed). The seat model that follows this ball is bl-5a98.

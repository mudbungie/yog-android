+++
title = "the phone says a dollar beside every token count, the workspace list is a ledger, and the admin surface prices a row and sets the ceiling (yog REMOTE §9.20, PROTOCOL 17)"
created = 1788745722
updated = 1788745722
priority = 3
root_commit = "b8421205e882caeadc666ccff26464e4e0f60dda"
tags = ["usability-r2"]
+++
Round 2, lane D2 — implementation child of yog bl-d13d (the design ball; ruling landed in yog docs/VISION.md §4.5 "Priced, and then said", docs/DESIGN.md §3.5 "Money everywhere tokens are said, and a ceiling that parks", and docs/REMOTE.md §9.20 "The wire says what it cost, and takes the rate"). Depends on the yog engine child (tagged usability-r2 in yog) landing first; until then build against REMOTE §9.20's stated shapes and the corpus.

## The contract this seat consumes

PROTOCOL is **17**. Four shapes this app already decodes gained a field, in the one figure shape the ball pane's `usd` already reads (`{"micro_usd","usd","unpriced_tokens"}`, ABSENT when the engine has no price table): `reply/steps` rows gain `cost`; `reply/science` attempts gain `cost`; `reply/rail` notches gain `cost` beside `budget`; `reply/workspaces` rows gain `spend` (the per-workspace ledger). One new window-level read, `Query::Prices` (`{"kind":"prices","rows":[{"provider","model","input","output","cache_read","cache_write"}],"ceiling":<usd>,"spent":<cost>}`, `ceiling`/`spent` absent when unset/unpriced, `"*"` a literal model meaning every model the row serves, `{}` rates = priced at zero, paints `$0.00`), and two acts: `Action::Price { provider, model, rates? }` (`/price <provider> <model> <input> <output> [<cache_read> [<cache_write>]]`, `off` deletes) and `Action::Ceiling { usd? }` (`/ceiling <usd>`, `/ceiling off`); both answer the re-derived `prices` reply, the ceiling's with `"released": <n>`.

## What this seat does

1. Codec: `codec/records/steps.rs` (today reads `tokens.total` only), `codec/records/spine.rs` (the notch), the candidates codec, and the workspace row gain the cost read through the ONE figure reader the ball pane already uses (`codec/balls.rs`' `usd` handling) — the seat never multiplies (yog REMOTE §9.7: derived text rides beside the fact; a seat paints `usd` verbatim). Add `codec/prices.rs`.
2. Paint: every place the app says `N tokens` — the records screen (§13.11), the candidates screen (§13.12), the ball pane (§13.9, already), the conversation header — says `N tokens — $x.yz` when `usd` is present, `N tokens` alone when absent, `at least $x.yz` when `unpriced_tokens` is non-zero. The workspace list gains the ledger: each workspace's `spend`, and the header the world's `spent` against `ceiling` off the `prices` read.
3. The admin surface (§13.17) gains the price table as rows with the two acts (price a `(provider, model)`, delete a row, set/clear the ceiling), with the same arming discipline that surface already uses for a destructive act (deleting a row is not destructive; clearing the ceiling is not either — no arming). The provider name offered comes from the wall's `providers` roster.
4. Stamp PROTOCOL 17; corpus regenerated; docs/DESIGN.md amended (§13.9/§13.11/§13.12/§13.17 gain the cost clause and the acts; the parity ledger file updated); the render-and-see loop (§15) states the `$` clause on a priced fixture.
5. Tests: decode with and without each gained field; a paint statement per surface (§15.2) proving the clause appears exactly when `usd` is present.

Done when a phone against a priced engine shows a dollar wherever it shows a token, the workspace list says what each workspace has cost, and the operator can price a row and set a ceiling from the glass.
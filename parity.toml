# **The parity exemption file** (yog docs/PARITY.md §7, bl-fe4c) — every op the
# engine classes `control` that this client does not surface, one line each,
# with the citation that makes the absence a decision rather than an oversight.
#
# The roster is not here: it is `corpus/reply/help.json`, vendored from yog,
# whose rows carry `surface` since protocol 7. This file is only the delta, and
# `src/parity` judges the two against the `act:` tags a `make screens` walk
# actually observed. Four assertions, and two of them keep THIS file honest —
# a line for an op that is now surfaced fails (stale), and a line naming an op
# the roster no longer classes `control` fails (rotted). Deleting a line
# re-reddens the gate and changes no code.
#
# **This file is where DESIGN §13.4's hand-kept ledger went**, and since the
# full-seat re-scope (bl-eac2, DESIGN §16.2) every line is one of two kinds:
# UNBUILT, citing the ball that builds the surface and deletes the line, or a
# structural reason that survives the full-seat ruling. The chat-loop-slice
# group reason (DESIGN §2) is retired and may not be cited again — a scope
# fence is not a reason once the operator has ruled the phone a full seat.
# The conformance decision table (`tests/conformance/expect.rs`) still records
# its §2 constants and stays correct: this codec spells none of these shapes
# yet, and each ball below moves exactly its own rows when it lands.

# --- Unbuilt: the fourth conversation act (bl-99fd) -------------------------
# bl-f97c landed three of this group's four on the conversation row's
# long-press menu (DESIGN §13.5) and deleted their lines. `fork` did not go
# with them, and the reason is a missing READ rather than a missing surface:
# its `from` is a fork point — a commit of the conversation's own history, or a
# `config/<name>` head — and the engine's own `fork::Attempt` says "Empty is
# not a value". The marks and the tip ride the `agent` read (bl-146b) and the
# lineage names ride `lineages` (bl-3685), both unbuilt, so a menu item for it
# could not fire. A free-text field where an operator types a commit sha on a
# phone is not the surface; it is this app asking the operator to be the read
# §8 forbids it to derive.
fork = "unbuilt: its fork point is a ref no read here names — the picking surface is bl-99fd (DESIGN §13.5)"

# --- Unbuilt: the held tool call (bl-b39d) ----------------------------------
answer = "unbuilt: the held-call surface — release, decline, keep parked (bl-b39d; DESIGN §16.2)"
revoke = "unbuilt: the auto-approval pair beside the held-call surface (bl-b39d; DESIGN §16.2)"
restore = "unbuilt: the auto-approval pair beside the held-call surface (bl-b39d; DESIGN §16.2)"

# --- Unbuilt: work review (bl-5a56) -----------------------------------------
files = "unbuilt: work review from the glass (bl-5a56; DESIGN §16.2)"
work-diff = "unbuilt: work review from the glass (bl-5a56; DESIGN §16.2)"

# --- Unbuilt: conversation machinery reads (bl-146b) ------------------------
agent = "unbuilt: a conversation machinery read (bl-146b; DESIGN §16.2)"
steps = "unbuilt: a conversation machinery read (bl-146b; DESIGN §16.2)"
step = "unbuilt: a conversation machinery read (bl-146b; DESIGN §16.2)"
rail = "unbuilt: a conversation machinery read (bl-146b; DESIGN §16.2)"
governing = "unbuilt: a conversation machinery read (bl-146b; DESIGN §16.2)"
inbox = "unbuilt: a conversation machinery read (bl-146b; DESIGN §16.2)"

# --- Unbuilt: the ball pane (bl-d587) ---------------------------------------
balls = "unbuilt: the ball pane (bl-d587; DESIGN §16.2)"
workspace-balls = "unbuilt: the ball pane (bl-d587; DESIGN §16.2)"
board = "unbuilt: the ball pane (bl-d587; DESIGN §16.2)"
close = "unbuilt: an act of the ball pane (bl-d587; DESIGN §16.2)"
assign = "unbuilt: an act of the ball pane (bl-d587; DESIGN §16.2)"
release = "unbuilt: an act of the ball pane (bl-d587; DESIGN §16.2)"
create = "unbuilt: an act of the ball pane (bl-d587; DESIGN §16.2)"
update = "unbuilt: an act of the ball pane (bl-d587; DESIGN §16.2)"

# --- Unbuilt: candidates (bl-2f17) ------------------------------------------
fan = "unbuilt: the candidate surfaces (bl-2f17; DESIGN §16.2)"
retire = "unbuilt: the candidate surfaces (bl-2f17; DESIGN §16.2)"
deliver = "unbuilt: the candidate surfaces (bl-2f17; DESIGN §16.2)"
science = "unbuilt: the candidate surfaces (bl-2f17; DESIGN §16.2)"

# --- Unbuilt: fleet and watch (bl-477e) -------------------------------------
fleet = "unbuilt: fleet and watch (bl-477e; DESIGN §16.2)"
disband = "unbuilt: fleet and watch (bl-477e; DESIGN §16.2)"
arm = "unbuilt: fleet and watch (bl-477e; DESIGN §16.2)"
disarm = "unbuilt: fleet and watch (bl-477e; DESIGN §16.2)"

# --- Unbuilt: trail and attention (bl-35bd) ---------------------------------
ops = "unbuilt: the trail surface (bl-35bd; DESIGN §16.2)"
ack = "unbuilt: the trail surface (bl-35bd; DESIGN §16.2)"
clear-trail = "unbuilt: the trail surface (bl-35bd; DESIGN §16.2)"
attention = "unbuilt: the whole-queue attention surface behind the per-row marks (bl-35bd; DESIGN §16.2)"

# --- Unbuilt: admin and armed deletions (bl-f645) ---------------------------
config = "unbuilt: admin surface (bl-f645; DESIGN §16.2)"
marks = "unbuilt: admin surface (bl-f645; DESIGN §16.2)"
scan = "unbuilt: admin surface — the engine's inbox flush; the app's own camera scan is not this op (bl-f645; DESIGN §16.2)"
delete-agent = "unbuilt: a typed-name-armed deletion (bl-f645; DESIGN §16.2)"
delete-workspace = "unbuilt: a typed-name-armed deletion (bl-f645; DESIGN §16.2)"

# --- Unbuilt: roster and discovery (bl-3685) --------------------------------
clients = "unbuilt: the roster surface — what this workspace's machines offer (bl-3685; DESIGN §16.2)"
lineages = "unbuilt: the lineage read (bl-3685; DESIGN §16.2)"
help = "unbuilt: the op-detail affordance over the vendored table, never a wire read (bl-3685; DESIGN §16.2)"

# --- Unbuilt: the minting seat (bl-2ee8) ------------------------------------
enroll = "unbuilt: the minting side — this seat fires the mint and displays the QR; re-classed from never by the full-seat ruling (bl-2ee8; DESIGN §16.2, amending §11)"

# --- Structural reasons that survive the full-seat ruling -------------------
follow = "read one shot at a time while a turn is in flight, never fired by a tap; the view it populates is the transcript, which act:transcript reaches (DESIGN §7)"
roles = "read at cadence to settle the controls row; what it populates is the selectors' own text, and no gesture asks for it (bl-e9f9)"

# --- Unbuilt before the re-scope, standing ----------------------------------
search = "in-wire and unsurfaced — DESIGN §13.4 blamed upstream and was wrong; a seat surface is unbuilt (bl-4c2b)"
seen = "unbuilt: this seat paints the attention mark on two screens and offers no gesture that answers it (bl-2889)"
